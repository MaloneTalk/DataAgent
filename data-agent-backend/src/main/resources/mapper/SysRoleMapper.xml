<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.github.malonetalk.mapper.SysRoleMapper">

    <resultMap id="BaseResultMap" type="io.github.malonetalk.entity.SysRole">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="description" property="description"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT * FROM sys_role ORDER BY id DESC
    </select>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT * FROM sys_role WHERE id = #{id}
    </select>

    <insert id="insert" parameterType="io.github.malonetalk.entity.SysRole" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_role (name, description, create_time, update_time)
        VALUES (#{name}, #{description}, #{createTime}, #{updateTime})
    </insert>

    <update id="update" parameterType="io.github.malonetalk.entity.SysRole">
        UPDATE sys_role
        <set>
            <if test="name != null">name = #{name},</if>
            <if test="description != null">description = #{description},</if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM sys_role WHERE id = #{id}
    </delete>

    <select id="countAll" resultType="int">
        SELECT COUNT(*) FROM sys_role
    </select>

</mapper>
