<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.github.malonetalk.mapper.RoleHiddenColumnMapper">

    <resultMap id="BaseResultMap" type="io.github.malonetalk.entity.RoleHiddenColumn">
        <id column="id" property="id"/>
        <result column="role_id" property="roleId"/>
        <result column="datasource_id" property="datasourceId"/>
        <result column="table_name" property="tableName"/>
        <result column="column_name" property="columnName"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <select id="selectByRoleAndDatasource" resultMap="BaseResultMap">
        SELECT * FROM role_hidden_column
        WHERE role_id = #{roleId} AND datasource_id = #{datasourceId}
        ORDER BY table_name, column_name
    </select>

    <insert id="insert"
            parameterType="io.github.malonetalk.entity.RoleHiddenColumn"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO role_hidden_column (role_id, datasource_id, table_name, column_name, create_time)
        VALUES (#{roleId}, #{datasourceId}, #{tableName}, #{columnName}, #{createTime})
    </insert>

    <delete id="deleteByRoleDatasourceAndTable">
        DELETE FROM role_hidden_column
        WHERE role_id = #{roleId}
          AND datasource_id = #{datasourceId}
          AND table_name = #{tableName}
    </delete>

    <delete id="deleteByRoleId">
        DELETE FROM role_hidden_column WHERE role_id = #{roleId}
    </delete>

</mapper>
