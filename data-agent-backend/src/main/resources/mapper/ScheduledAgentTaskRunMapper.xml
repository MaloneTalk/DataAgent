<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.github.malonetalk.mapper.ScheduledAgentTaskRunMapper">

    <resultMap id="BaseResultMap" type="io.github.malonetalk.entity.ScheduledAgentTaskRun">
        <id column="id" property="id"/>
        <result column="task_id" property="taskId"/>
        <result column="session_id" property="sessionId"/>
        <result column="status" property="status"/>
        <result column="report_id" property="reportId"/>
        <result column="output_summary" property="outputSummary"/>
        <result column="error_message" property="errorMessage"/>
        <result column="started_at" property="startedAt"/>
        <result column="finished_at" property="finishedAt"/>
    </resultMap>

    <insert id="insert" parameterType="io.github.malonetalk.entity.ScheduledAgentTaskRun"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO scheduled_agent_task_run (
            task_id, session_id, status, started_at
        ) VALUES (
            #{taskId}, #{sessionId}, #{status}, #{startedAt}
        )
    </insert>

    <update id="finish">
        UPDATE scheduled_agent_task_run
        SET status = #{status},
            report_id = #{reportId},
            output_summary = #{outputSummary},
            error_message = #{errorMessage},
            finished_at = #{finishedAt}
        WHERE id = #{id}
    </update>

    <select id="selectByTaskId" resultMap="BaseResultMap">
        SELECT * FROM scheduled_agent_task_run
        WHERE task_id = #{taskId}
        ORDER BY id DESC
        LIMIT #{limit}
    </select>
</mapper>
