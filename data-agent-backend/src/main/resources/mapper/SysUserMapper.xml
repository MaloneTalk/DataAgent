<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.github.malonetalk.mapper.SysUserMapper">

    <resultMap id="BaseResultMap" type="io.github.malonetalk.entity.SysUser">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="password_hash" property="passwordHash"/>
        <result column="display_name" property="displayName"/>
        <result column="role_id" property="roleId"/>
        <result column="is_super_admin" property="superAdmin"/>
        <result column="idp_type" property="idpType"/>
        <result column="idp_user_id" property="idpUserId"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <select id="selectByUsername" resultMap="BaseResultMap">
        SELECT * FROM sys_user
        WHERE username = #{username} AND idp_type = 'LOCAL'
    </select>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT * FROM sys_user WHERE id = #{id}
    </select>

    <insert id="insert" parameterType="io.github.malonetalk.entity.SysUser" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_user (
            username, password_hash, display_name, role_id, is_super_admin,
            idp_type, idp_user_id, status, create_time, update_time
        ) VALUES (
            #{username}, #{passwordHash}, #{displayName}, #{roleId}, COALESCE(#{superAdmin}, 0),
            #{idpType}, #{idpUserId}, #{status}, #{createTime}, #{updateTime}
        )
    </insert>

    <update id="updatePassword">
        UPDATE sys_user
        SET password_hash = #{passwordHash}, update_time = #{updateTime}
        WHERE id = #{id}
    </update>

    <select id="existUser" resultType="boolean">
        SELECT EXISTS(SELECT 1 FROM sys_user)
    </select>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT * FROM sys_user ORDER BY id DESC
    </select>

    <update id="update" parameterType="io.github.malonetalk.entity.SysUser">
        UPDATE sys_user
        <set>
            <if test="displayName != null">display_name = #{displayName},</if>
            <if test="roleId != null">role_id = #{roleId},</if>
            <if test="superAdmin != null">is_super_admin = #{superAdmin},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
        </set>
        WHERE id = #{id}
    </update>

    <update id="updateStatus">
        UPDATE sys_user SET status = #{status}, update_time = NOW() WHERE id = #{id}
    </update>

</mapper>
